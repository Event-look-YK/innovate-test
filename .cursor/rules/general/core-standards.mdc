---
description: Code style, naming, and key conventions for the project
alwaysApply: true
---

# Core Standards

- **Rule scope:** This file applies across the monorepo. Frontend-specific guidance (FSD layout, shadcn/Tailwind, TanStack Query usage, web env, Zustand stores in the web app) lives under **`.cursor/rules/web/`** and is attached only when paths match each rule’s **`globs`** (mainly **`apps/web/**`** and **`packages/ui/**`**).

- Write concise, technical TypeScript with accurate examples.
- Use functional and declarative patterns; avoid classes.
- Prefer iteration and modularization over duplication.
- Use descriptive variable names with auxiliary verbs (e.g. `isLoading`, `hasError`).
- Avoid comments in code; prefer self-documenting names and structure.
- Prefer types over interfaces.

## Naming and layout

- Files and folders: **kebab-case** (e.g. `use-user.ts`, `components/user-profile-card`).
- Favor named exports for components and public API (`export const user = {};`).

## Modules and imports

- Avoid **barrel** `index.ts` files that re-export entire folders; import concrete modules (e.g. `@/shared/lib/http`, `@/features/auth/ui/login-form`).
- In the **web app** (`apps/web`), use `@/shared/...`, `@/features/<domain>/...`, `@/views/<domain>/...` (alias maps to `apps/web/src/*`).
- Shared packages:
  - `@innovate-test/ui/components/<name>` — shadcn/Radix UI components
  - `@innovate-test/env/web` — typed web env vars
  - `@innovate-test/env/server` — typed server env vars
  - `@innovate-test/db` — Drizzle ORM client and schema
  - `@innovate-test/auth` — Better Auth instance
- Local persistence in the web app: **Dexie** for IndexedDB + **TanStack DB** for reactive collections; keep schema and collection setup under `shared/db/`.

## Syntax

- Prefer **arrow functions** over `function` declarations (e.g. `const fn = () => {}` instead of `function fn() {}`).
- Prefer concise conditionals; avoid unnecessary braces for simple statements.

## TypeScript and Zod

- Always use TypeScript types; avoid `any` and `unknown` without strong justification.
- Co-locate Zod schemas with the data they validate and derive types from schemas (e.g. `export type User = z.infer<typeof userSchema>`).

## Functions and complexity

- Split complex logic into small, focused functions with single responsibility.
- Prefer pure functions and composition over large, stateful blocks.

## Control flow

- Use early returns and guard clauses to avoid deep nesting.
- Don't use comments in code.